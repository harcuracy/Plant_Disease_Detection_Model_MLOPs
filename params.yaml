data_ingestion:
  validation_split: 0.2
  max_images_per_class: 100
  random_seed: 42

prepare_base_model:
  image_size: [224, 224, 3]
  learning_rate: 0.0001
  include_top: false
  weights: imagenet
  classes: 38
  dropout_rate: 0.2

training:
  batch_size: 16
  epochs: 20
  augmentation: true
